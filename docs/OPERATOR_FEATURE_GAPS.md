# Operator Feature / Gap Register — OCC-3M

This register records gaps discovered by the adversarial release-certification program. A gap is closed only when the fix exists at the correct authority boundary and has regression evidence.

| ID | Severity | Status | Gap | Resolution / remaining action |
|---|---|---|---|---|
| FG-001 | P1 | RESOLVED | Shared-VPS Operator gateway repair was live-only and not reproducible from Operator source. | PR #20 source-controlled the shared-VPS compose + internal Caddy contract, exact `.10 -> .20` trust topology, pinned Caddy, and `NET_BIND_SERVICE` requirement. CI and real Caddy parser passed. |
| FG-002 | P1 | RESOLVED-LIVE | TLS SNI `operator.splcart.in` with HTTP `Host: evil.example` returned an empty 200 from the outer shared Caddy. | Added global `strict_sni_host on`; exact failure now returns 421 while Operator, SPLCART, attendance and auth remain 200. Both active and canonical VPS Caddyfiles were backed up and patched. |
| FG-003 | P2 | RESOLVED-LOCAL-SOURCE | A future SPLCART deploy could previously erase the Operator/Auth vhosts, strict SNI policy and `operator_ingress` attachment. | Persisted the exact proven live topology in SPLCART source commit `e1d2207cf8ff05de7954b69480ef8a77e3f7077b` and added `scripts/check-shared-ingress-contract.cjs` to deployment-readiness CI. The local SPLCART repository currently has no Git remote; remote backup is an operational follow-up, not a release-behavior gap. |
| FG-004 | P0-release | BLOCKED-EXTERNAL | Real OAuth token audience/scopes have not been observed from a completed production authorization-code flow. | Complete one human login/consent, exchange the code with PKCE, verify `aud=https://operator.splcart.in/mcp`, issuer, expiry and scopes, then invoke `/mcp`. |
| FG-005 | P2-release | BLOCKED-EXTERNAL | OpenAI domain verification token has not been issued. | OCC-3M is configuration-ready via `OPENAI_APPS_CHALLENGE_TOKEN`; keep the endpoint 404 until the portal supplies the exact token, then reload only the edge/gateway environment and verify the exact response. |
| FG-006 | P2-release | BLOCKED-HUMAN | Reviewer credentials/pairing are not yet certified for OpenAI review. | The deterministic non-sensitive fixture is locally certified at baseline `ab658c353fc3e0ce79d71e2968f53eedbc247537`: exact 5 positive + 3 negative public-boundary cases pass on Node 22.23.2. `OPERATOR_REVIEWER_AUTH_EXECUTION.md` defines the backup, interactive Argon2id hashing, reviewer-only OIDC policy, validation, activation, isolation, pairing and rollback procedure. The proposed custom reviewer policy passed isolated `authelia config validate` against exact production Authelia 4.39.26 wiring. A disposable reviewer user record also passes Authelia's published v4.39 user-database JSON Schema, while an intentionally malformed record is rejected. A dedicated production reviewer account and reviewer-only one-factor authorization policy are now provisioned; normal users retain the policy default of two-factor, Authelia is healthy, and the credential value was not exposed to Git/chat. The password handoff remains owner-controlled and has not been used by this certification agent. Remaining work is owner retrieval/login verification, canonical fixture-only device pairing, and real OAuth/ChatGPT reviewer execution. |
| FG-007 | P2-release | BLOCKED-EXTERNAL | OpenAI Scan Tools has not imported the final production MCP metadata. | After the final Mecord Connect successor is deployed, run Scan Tools and reconcile names, titles, descriptions, schemas, security schemes, annotations, `_meta`, instructions and domains against that exact deployed 9-tool SHA. Do not certify the frozen OCC-3M 10-tool snapshot as the final submission surface. |
| FG-008 | P2-release | BLOCKED-EXTERNAL | Real ChatGPT end-to-end read/write workflow has not run through production OAuth -> MCP -> relay -> paired PC. | Connect the draft in ChatGPT and execute at least one read-only and one write/approval flow, then revoke/reconnect and verify failure/recovery behavior. |
| FG-009 | P2-release | BLOCKED-EXTERNAL | The owner has selected the individual publisher identity **Kinthala Samuel Sampath Kumar**, but OpenAI Platform individual verification for that exact identity is not yet proven. | Complete OpenAI individual publisher verification. Before submission, compare the exact verified legal-name spelling/order against source/public metadata and reconcile any difference. |
| FG-010 | P3 | DOCUMENTED | Operator's convenience mirror at `/.well-known/oauth-authorization-server` does not echo `token_endpoint_auth_methods_supported`. | Not a blocker: protected-resource metadata points to `https://auth.splcart.in`, whose authoritative discovery advertises `none` and other accepted methods. Consider mirroring it in a later cleanup release. |
| FG-011 | P1-release | BLOCKED-EXTERNAL-PUBLICATION | The public runtime is the unscoped **mecord-connect@1.0.0** package. npm account **mecrod** and 2FA mode **auth-and-writes** were previously verified; a fresh authenticated publication session is still required immediately before the irreversible first publish. No npm organization/scope is required. The registry still returns 404 before first publication. | Account/2FA setup is resolved. Remaining work is explicit owner authorization to publish the exact certified first-release tarball, public-registry verification, and clean-machine `npx mecord-connect@latest doctor` plus reviewer-root startup proof. |
| FG-012 | P2 | RESOLVED | Reviewer positive #5 previously required destructive `file.replace`, which correctly needs local one-time approval and could make the reviewer case non-self-service. | Replaced positive #5 with bounded `git.diff`; kept `file.replace` approval as an internal security assertion. Local reviewer harness passes all submitted 5 positive + 3 negative cases plus `APPROVAL_REQUIRED` and `TARGET_EXISTS` checks. |
| FG-013 | P1-release | RESOLVED-SOURCE | The owner selected **PROPRIETARY** licensing. The public package now declares `SEE LICENSE IN LICENSE` and ships an owner-specific proprietary `LICENSE` granting bounded authorized use of the Mecord Connect runtime. | Source guards and npm tests require the proprietary license and include it in the tarball. Final closure still depends on the exact final package being published/verified and the matching live legal surface being deployed, but the owner licensing decision itself is resolved. |
| FG-014 | P3 | RESOLVED-SOURCE | Public npm metadata/support/policy discoverability is complete in the candidate: support/issues metadata, deliberate keywords, publisher identity, Support/Privacy/Terms links, proprietary LICENSE notice and dual-use DISCLOSURE notice are present. | Regression tests require the canonical metadata and README policy links. Final registry verification occurs after first publication. |
| FG-015 | P1-release | RESOLVED-SOURCE | The owner selected **DUAL_USE**. The unscoped `mecord-connect` package requires `contentPolicy.class = "dual-use"`, ships root `DISCLOSURE`, and fails closed if the declaration is missing even on the first unpublished release. | Full-history continuity remains enabled. npm account/2FA setup is now independently verified; final closure depends only on exact first publication/registry verification under the explicit release-authorization gate. |
| FG-016 | P1-release | RESOLVED-LIVE | Authelia advertises authorization-response issuer identification, so current OpenAI OAuth can select the stable ChatGPT redirect, but the production predefined client initially allowlisted only the callback-ID-specific redirect. | Reproduced before repair: callback-ID redirect entered login while the stable redirect failed `invalid_request` for redirect mismatch. Added `https://chatgpt.com/connector_platform_oauth_redirect` alongside the existing callback-ID URI after isolated production-version config validation; recreated only `operator-auth`; both redirect forms now enter login, OIDC discovery remains healthy, and MCP still fails closed with the expected unauthenticated 401. Rollback hashes and semantic-only-diff evidence are recorded in `OPERATOR_REAL_OAUTH_PROOF.md`. OCC-3M source/image identity is unchanged. |
| FG-017 | P1-release | RESOLVED-SOURCE-BLOCKED-VERIFICATION | The owner selected the individual publisher identity **Kinthala Samuel Sampath Kumar**, public locality **Akkayapalem, Visakhapatnam, Andhra Pradesh, India**, support **support@splcart.in**, and India/Visakhapatnam governing-law wording. Source plugin metadata, npm author/license/disclosure, and production-notice sources now use that identity. | Complete OpenAI individual publisher verification and ensure the exact verified legal-name spelling/order matches. Then deploy the final Mecord Connect legal pages and verify live website/support/privacy/terms consistency. If OpenAI verifies a different legal spelling/order, reconcile source/public metadata before submission. |
| FG-018 | P1-release | RESOLVED-INTEGRATED | Public Privacy/Terms/Support notice bytes were previously absent from source despite being required by the shared-VPS bind mount. | The notice reproducibility work from PR #24 is integrated into current `main`; PR #28 hardening is merged and PR #29 removes the final standalone legacy Operator branding from the source-controlled production notices. The current source candidate controls the Mecord Connect notices and the shared-VPS bind-mount contract; owner identity/license/policy decisions are already applied. Final live deployment remains separately tracked by FG-021. |
| FG-019 | P1-release | RESOLVED-INTEGRATED | The old OCC-3M manifest lacked the required final Mecord Connect logo/composer-icon and public product identity. | Current `main` `d51a3b2ee4746e1a29f9cf2ec87cef603ac76edf` carries the dedicated Mecord Connect square SVG, exact manifest identity, OAuth `resource_name`, unified MCP `name/title/version`, generated landing/legal titles, source notices, reviewer/submission naming, publisher metadata and the canonical 9-tool public allowlist. The hardening payload merged from PR #28 after CI #911, Platform Matrix #681, NPM Runtime #311 and Windows Signing Smoke #682 all passed; PR #29 notice cleanup independently passed CI #914, Platform Matrix #684 and Signing #685. MSIX/Store remains optional and outside Mecord Connect v1. |
| FG-020 | P1-release | BLOCKED-REAL-E2E | Current OpenAI final submission requirements for remote-MCP plugins require a demo recording URL showing the major use cases across supported platforms. Older Operator evidence incorrectly treated the recording as optional. | `OPERATOR_DEMO_RECORDING_RUNBOOK.md` now defines the exact production-only sequence, privacy constraints, hosting requirements and closure rule. Produce the recording only after the real ChatGPT OAuth/reviewer flow is working from the public package. Host it at a reviewer-accessible URL and add that URL to the final submission package. |
| FG-021 | P1-release | BLOCKED-FINAL-DEPLOYMENT | The final source candidate is branded Mecord Connect and now contains the owner-approved publisher/legal wording, but production intentionally remains OCC-3M and still serves the pre-rebrand/pre-final legal pages. | After the remaining npm/OpenAI/reviewer gates are ready, deploy the final Mecord Connect successor transactionally, verify `/`, `/privacy`, `/terms`, `/support`, OAuth metadata and MCP behavior, then rerun hostile/public-page checks and Scan Tools on the deployed final SHA. |

## Closed production drift

Before OCC-3M, production Operator was still executing the old `60813bde...` edge while the certified source had advanced. OCC-3M deployment replaced that drift with the exact merge source `405be7a...` and image `sha256:7f1114f7...`. Production source/image identity is now directly evidenced.

## Feature-creep rule

No new public capability is added merely because it is convenient. A new public tool or permission must be justified by a concrete user workflow, mapped to OpenAI policy, have least-privilege OAuth/security metadata, failure/approval semantics, privacy projection, adversarial tests, and fresh Scan Tools review before release.
