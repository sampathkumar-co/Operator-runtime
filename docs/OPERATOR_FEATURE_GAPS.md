# Operator Feature / Gap Register — OCC-3M

This register records gaps discovered by the adversarial release-certification program. A gap is closed only when the fix exists at the correct authority boundary and has regression evidence.

| ID | Severity | Status | Gap | Resolution / remaining action |
|---|---|---|---|---|
| FG-001 | P1 | RESOLVED | Shared-VPS Operator gateway repair was live-only and not reproducible from Operator source. | PR #20 source-controlled the shared-VPS compose + internal Caddy contract, exact `.10 -> .20` trust topology, pinned Caddy, and `NET_BIND_SERVICE` requirement. CI and real Caddy parser passed. |
| FG-002 | P1 | RESOLVED-LIVE | TLS SNI `operator.splcart.in` with HTTP `Host: evil.example` returned an empty 200 from the outer shared Caddy. | Added global `strict_sni_host on`; exact failure now returns 421 while Operator, SPLCART, attendance and auth remain 200. Both active and canonical VPS Caddyfiles were backed up and patched. |
| FG-003 | P2 | RESOLVED-LOCAL-SOURCE | A future SPLCART deploy could previously erase the Operator/Auth vhosts, strict SNI policy and `operator_ingress` attachment. | Persisted the exact proven live topology in SPLCART source commit `e1d2207cf8ff05de7954b69480ef8a77e3f7077b` and added `scripts/check-shared-ingress-contract.cjs` to deployment-readiness CI. The local SPLCART repository currently has no Git remote; remote backup is an operational follow-up, not a release-behavior gap. |
| FG-004 | P0-release | BLOCKED-EXTERNAL | Real OAuth token audience/scopes have not been observed from a completed production authorization-code flow. | Complete one human login/consent, exchange the code with PKCE, verify `aud=https://operator.splcart.in/mcp`, issuer, expiry and scopes, then invoke `/mcp`. |
| FG-005 | P2-release | BLOCKED-EXTERNAL | OpenAI domain verification token has not been issued. | OCC-3M is configuration-ready via `OPENAI_APPS_CHALLENGE_TOKEN`; keep the endpoint 404 until the portal supplies the exact token, then reload only the edge/gateway environment and verify the exact response. |
| FG-006 | P2-release | BLOCKED-HUMAN | Reviewer credentials/pairing are not yet certified for OpenAI review. | The deterministic non-sensitive fixture is locally certified at baseline `ab658c353fc3e0ce79d71e2968f53eedbc247537`: exact 5 positive + 3 negative public-boundary cases pass on Node 22.23.2. `OPERATOR_REVIEWER_AUTH_EXECUTION.md` defines the backup, interactive Argon2id hashing, reviewer-only OIDC policy, validation, activation, isolation, pairing and rollback procedure. The proposed custom reviewer policy passed isolated `authelia config validate` against exact production Authelia 4.39.26 wiring. A disposable reviewer user record also passes Authelia's published v4.39 user-database JSON Schema, while an intentionally malformed record is rejected. A dedicated production reviewer account and reviewer-only one-factor authorization policy are now provisioned; normal users retain the policy default of two-factor, Authelia is healthy, and the credential value was not exposed to Git/chat. The password handoff remains owner-controlled and has not been used by this certification agent. Remaining work is owner retrieval/login verification, canonical fixture-only device pairing, and real OAuth/ChatGPT reviewer execution. |
| FG-007 | P2-release | BLOCKED-EXTERNAL | OpenAI Scan Tools has not imported the production MCP metadata. | Run Scan Tools after draft/auth configuration; reconcile names, titles, descriptions, schemas, security schemes, annotations, `_meta`, instructions and domains against OCC-3M. |
| FG-008 | P2-release | BLOCKED-EXTERNAL | Real ChatGPT end-to-end read/write workflow has not run through production OAuth -> MCP -> relay -> paired PC. | Connect the draft in ChatGPT and execute at least one read-only and one write/approval flow, then revoke/reconnect and verify failure/recovery behavior. |
| FG-009 | P2-release | BLOCKED-EXTERNAL | Publisher identity verification state is not proven. | Complete individual or business verification in the OpenAI Platform Dashboard for the exact directory publisher name. |
| FG-010 | P3 | DOCUMENTED | Operator's convenience mirror at `/.well-known/oauth-authorization-server` does not echo `token_endpoint_auth_methods_supported`. | Not a blocker: protected-resource metadata points to `https://auth.splcart.in`, whose authoritative discovery advertises `none` and other accepted methods. Consider mirroring it in a later cleanup release. |
| FG-011 | P1-release | BLOCKED-EXTERNAL | The documented clean-machine runtime `@mecrod/operator@1.0.0` is not published; npm registry returns E404 and this machine has no authenticated npm session to prove `@mecrod` scope ownership. | PR #22 / OCC-3N head `bbcc12a7b5167345ab865af3f6c230580a2eda37` provides the tested artifact-only first-release path and exact-tarball staged path for later releases; CI #700, Platform #470, NPM Runtime #128 and Signing #471 all pass. npm 11.19.1 also accepts the exact local `.tgz` package-spec in `npm stage publish --dry-run`, proving the future staging command shape. `OPERATOR_NPM_NAMESPACE_BOOTSTRAP.md` defines the authenticated org/2FA/first-publish verification flow. Remaining work is external owner execution plus FG-013/FG-015 resolution and clean-machine verification. |
| FG-012 | P2 | RESOLVED | Reviewer positive #5 previously required destructive `file.replace`, which correctly needs local one-time approval and could make the reviewer case non-self-service. | Replaced positive #5 with bounded `git.diff`; kept `file.replace` approval as an internal security assertion. Local reviewer harness passes all submitted 5 positive + 3 negative cases plus `APPROVAL_REQUIRED` and `TARGET_EXISTS` checks. |
| FG-013 | P1-release | BLOCKED-OWNER-DECISION | The public npm package declares `license: UNLICENSED`, the public GitHub repository has no license, and no bundled software license grants users the right to install/use the runtime. | `OPERATOR_NPM_LICENSE_DECISION.md` compares proprietary custom, Apache-2.0 and MIT paths and recommends a proprietary custom runtime license when the intent is a publicly installable connector for the hosted Operator service without broad redistribution/repackaging rights. The owner must accept a model/final wording; then apply the license to PR #22's successor and rerun affected certification. |
| FG-014 | P3 | PARTIAL-DRAFT | Public npm metadata lacked a `bugs`/support field and package README did not link support, Terms, Privacy, or license information; keywords/author metadata were also absent. | PR #22 adds support/issues metadata, deliberate keywords, and Support/Privacy/Terms/Issues README links. The remaining license notice/link depends on FG-013, so this gap is not marked fully resolved until the owner-approved license is added to the final successor tarball. |
| FG-015 | P1-release | BLOCKED-OWNER-POLICY-DECISION | npm's current Dual-Use Content Policy requires declaration for security-relevant packages whose capabilities can resemble malicious software; the examples are non-exhaustive. Operator ships security-sensitive remote-computer automation, but its classification has not been decided. | PR #22 removes automatic direct `npm publish`, makes first release artifact-only, requires exact-tarball staging for later versions, explicitly permits root `DISCLOSURE` through the package files allowlist, fail-closes if a declared dual-use tarball omits it, and checks a bounded full npm package-history manifest so any published dual-use declaration must persist even if it was under a non-`latest` tag. Registry identity/shape and unknown historical policy classes also fail closed. Synthetic owner-path probes prove both `LICENSE`-only and `LICENSE` + `DISCLOSURE` tarballs pack correctly. `OPERATOR_NPM_POLICY_CLASSIFICATION.md` retains the owner/npm classification decision. |
| FG-016 | P1-release | RESOLVED-LIVE | Authelia advertises authorization-response issuer identification, so current OpenAI OAuth can select the stable ChatGPT redirect, but the production predefined client initially allowlisted only the callback-ID-specific redirect. | Reproduced before repair: callback-ID redirect entered login while the stable redirect failed `invalid_request` for redirect mismatch. Added `https://chatgpt.com/connector_platform_oauth_redirect` alongside the existing callback-ID URI after isolated production-version config validation; recreated only `operator-auth`; both redirect forms now enter login, OIDC discovery remains healthy, and MCP still fails closed with the expected unauthenticated 401. Rollback hashes and semantic-only-diff evidence are recorded in `OPERATOR_REAL_OAUTH_PROOF.md`. OCC-3M source/image identity is unchanged. |
| FG-017 | P1-release | BLOCKED-OWNER-IDENTITY | The live Operator Privacy/Terms pages identify the service only as operated by "the publisher of splcart.in" rather than naming the exact legal publisher. The main SPLCART Terms also explicitly say they still require review against the actual business entity/jurisdiction, and the storefront Contact page says direct support details are not published. This prevents proving that the public legal identity matches the OpenAI publisher-verification identity. | Owner must provide/approve the exact legal publisher person/entity, governing jurisdiction and public contact identity. Exact patch targets are now known: Operator canonical notice targets are `deploy/public-edge/production-notices/{privacy,terms,support}.md` (live equivalents under `/home/deploy/operator/ops/production-notices/`); SPLCART storefront identity/legal text is in `frontend-new/src/lib/trust-content.ts` and `frontend-new/src/components/trust/PublicContentPage.tsx`; and final plugin metadata must reconcile `.codex-plugin/plugin.json` `author.name` / `interface.developerName` with the owner-approved verified publisher identity rather than blindly treating the Mecord Connect product brand as the legal publisher identity if they differ. Then verify the live pages and use the exact same identity in OpenAI publisher verification and the proprietary-license Licensor field if applicable. Do not invent an entity from repository/account names. |
| FG-018 | P1-release | RESOLVED-DRAFT | The canonical shared-VPS compose bind-mounts `./production-notices`, but `deploy/public-edge/production-notices/` was explicitly ignored and absent from GitHub `main`, so a clean checkout could not reproduce the live reviewer-facing Privacy/Terms/Support content. | Draft PR #24 / OCC-3O head `6edd3ee8eca2f5b8096424bac8750f0c945cec94` removes only the notice-directory ignore rule, source-controls the exact currently live three notices, and adds regression coverage tying the files to the compose bind mount. Exact-head GitHub validation is green: CI #710, Platform Matrix #480 and Windows Signing Smoke #481 PASS. Local deployment tests are 6/6 PASS, shared-VPS Compose resolves from tracked source, notice secret-like scan is 0 hits, and all three source files match live production SHA-256 byte-for-byte. A disposable merge preview with frozen npm-hardening PR #22 (`bbcc12a7b5167345ab865af3f6c230580a2eda37`) was conflict-free, passed focused combined tests 24/24, and passed the full Windows-aware suite at 367 tests / 351 pass / 16 expected skips / 0 fail. Keep both PRs draft until FG-013/FG-015/FG-017 owner decisions are applied to the eventual composed successor. |
| FG-019 | P1-release | RESOLVED-DRAFT | Current OpenAI directory validation requires both `interface.logo` and `interface.composerIcon`, and the old OCC-3M manifest had neither field or the final Mecord Connect identity. | Integrated PR #27 now carries a dedicated Mecord Connect 64x64 SVG mark, exact manifest identity, OAuth protected-resource `resource_name`, public MCP initialize `name/title`, generated landing/legal-page titles, source notice headings, reviewer/submission naming, and a 10-tool README count; the old SPLCART favicon/public-plugin identity is removed. Current Mecord Connect head is `c0a87af4166a626b4aaea250438caa9fdbb00b08`; exact-head CI #745, Platform Matrix #515, NPM Runtime #156 and Windows Signing Smoke #516 all PASS. Local modified MCP E2E is 7 pass / 1 expected Windows symlink skip / 0 fail, the focused release suite is 28/28 PASS, and the full Windows-aware suite is 368 tests / 352 pass / 16 expected skips / 0 fail. The Microsoft Store/MSIX display/artifact identity remains a separate distribution lane and is intentionally not renamed by this plugin rebrand. |
| FG-020 | P1-release | BLOCKED-REAL-E2E | Current OpenAI final submission requirements for remote-MCP plugins require a demo recording URL showing the major use cases across supported platforms. Older Operator evidence incorrectly treated the recording as optional. | `OPERATOR_DEMO_RECORDING_RUNBOOK.md` now defines the exact production-only sequence, privacy constraints, hosting requirements and closure rule. Produce the recording only after the real ChatGPT OAuth/reviewer flow is working from the public package. Host it at a reviewer-accessible URL and add that URL to the final submission package. |
| FG-021 | P1-release | BLOCKED-FINAL-DEPLOYMENT | The integrated candidate and submission package are branded **Mecord Connect**, but production intentionally remains OCC-3M and its public reviewer pages still carry the pre-rebrand SPLCART Operator product name. | Do not submit with mixed branding. After FG-013/FG-015/FG-017 and the final successor are resolved, deploy the final Mecord Connect successor transactionally, then verify `/`, `/privacy`, `/terms`, and `/support` all show Mecord Connect product branding while the owner-approved legal publisher identity remains accurate. Re-run hostile/public-page checks and Scan Tools on that deployed final SHA. |

## Closed production drift

Before OCC-3M, production Operator was still executing the old `60813bde...` edge while the certified source had advanced. OCC-3M deployment replaced that drift with the exact merge source `405be7a...` and image `sha256:7f1114f7...`. Production source/image identity is now directly evidenced.

## Feature-creep rule

No new public capability is added merely because it is convenient. A new public tool or permission must be justified by a concrete user workflow, mapped to OpenAI policy, have least-privilege OAuth/security metadata, failure/approval semantics, privacy projection, adversarial tests, and fresh Scan Tools review before release.
