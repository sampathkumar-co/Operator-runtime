{
  "name": "operator-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "test:red-team": "node --experimental-strip-types --test security/*.test.ts",
    "check": "node --experimental-strip-types scripts/check.ts && npm test",
    "dev:agent": "node --experimental-strip-types apps/local-agent/src/main.ts"
  },
  "license": "UNLICENSED",
  "description": "Semantic, evidence-driven execution runtime for operating user-authorized computers from ChatGPT."
}
